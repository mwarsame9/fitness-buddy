
<% if current_user %>
<h4>Measurements</h4>
<ul>
  <% @measurements.each do |measurement| %>
    <li><%= link_to "Show", measurement_path(measurement) %></li>
  <% end %>
</ul>
<hr>
<%= link_to "New Measurement", new_measurement_path %>

<% else %>
<%= render "index" %>
<% end %>
